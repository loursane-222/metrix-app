diff --git a/prisma/schema.prisma b/prisma/schema.prisma
index 245ef4f..bef0af9 100644
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -8,21 +8,26 @@ datasource db {
 }
 
 model User {
-  id            String   @id @default(cuid())
-  email         String   @unique
+  id            String    @id @default(cuid())
+  email         String    @unique
   password      String
-  ad            String   @default("")
-  aktif         Boolean  @default(true)
+  ad            String    @default("")
+  aktif         Boolean   @default(true)
   abonelikBitis DateTime?
-  createdAt     DateTime @default(now())
-
-  atolye Atolye?
+  role          String    @default("ADMIN")
+  personelId    String?   @unique
+  allowedMenus  String[]  @default([])
+  createdAt     DateTime  @default(now())
+
+  atolye          Atolye?
+  personel        Personel?        @relation(fields: [personelId], references: [id])
+  menuPermissions MenuPermission[]
 }
 
 model Atolye {
-  id        String @id @default(cuid())
-  userId    String @unique
-  user      User   @relation(fields: [userId], references: [id])
+  id     String @id @default(cuid())
+  userId String @unique
+  user   User   @relation(fields: [userId], references: [id])
 
   atolyeAdi String @default("")
   sehir     String @default("")
@@ -108,17 +113,17 @@ model Arac {
 }
 
 model Musteri {
-  id        String   @id @default(cuid())
-  atolyeId  String
-  atolye    Atolye   @relation(fields: [atolyeId], references: [id])
+  id       String @id @default(cuid())
+  atolyeId String
+  atolye   Atolye @relation(fields: [atolyeId], references: [id])
 
-  firmaAdi        String   @default("")
-  ad              String   @default("")
-  soyad           String   @default("")
-  telefon         String   @default("")
-  email           String   @default("")
-  acilisBakiyesi  Decimal  @default(0)
-  bakiyeTipi      String   @default("borc")
+  firmaAdi       String  @default("")
+  ad             String  @default("")
+  soyad          String  @default("")
+  telefon        String  @default("")
+  email          String  @default("")
+  acilisBakiyesi Decimal @default(0)
+  bakiyeTipi     String  @default("borc")
 
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
@@ -128,12 +133,12 @@ model Musteri {
 }
 
 model Tahsilat {
-  id        String   @id @default(cuid())
+  id        String  @id @default(cuid())
   musteriId String
-  musteri   Musteri  @relation(fields: [musteriId], references: [id], onDelete: Cascade)
+  musteri   Musteri @relation(fields: [musteriId], references: [id], onDelete: Cascade)
 
-  tarih     DateTime
-  tutar     Decimal  @default(0)
+  tarih DateTime
+  tutar Decimal  @default(0)
 
   createdAt DateTime @default(now())
 }
@@ -152,14 +157,14 @@ model Is {
   malzemeTipi String
   musteriTipi String
 
-  plakaFiyatiEuro    Decimal
-  metrajMtul         Decimal
-  birMtulDakika      Decimal
-  tezgahArasiMtul    Decimal @default(0)
-  tezgahArasiDakika  Decimal @default(0)
-  adaTezgahMtul      Decimal @default(0)
-  adaTezgahDakika    Decimal @default(0)
-  kullanilanKur      Decimal @default(0)
+  plakaFiyatiEuro   Decimal
+  metrajMtul        Decimal
+  birMtulDakika     Decimal
+  tezgahArasiMtul   Decimal @default(0)
+  tezgahArasiDakika Decimal @default(0)
+  adaTezgahMtul     Decimal @default(0)
+  adaTezgahDakika   Decimal @default(0)
+  kullanilanKur     Decimal @default(0)
 
   plakaGenislikCm    Decimal @default(0)
   plakaUzunlukCm     Decimal @default(0)
@@ -179,8 +184,8 @@ model Is {
   mtulSatisFiyati  Decimal @default(0)
 
   teklifGecerlilikTarihi DateTime?
-  kirilanTasPlaka        Decimal @default(0)
-  hataliKesimPlaka       Decimal @default(0)
+  kirilanTasPlaka        Decimal   @default(0)
+  hataliKesimPlaka       Decimal   @default(0)
 
   durum String @default("teklif_verildi")
 
@@ -188,7 +193,7 @@ model Is {
 
   isTarihi DateTime?
 
-  tahsilat         Decimal  @default(0)
+  tahsilat         Decimal   @default(0)
   onaylanmaTarihi  DateTime?
   kaybedilmeTarihi DateTime?
   notlar           String?
@@ -196,8 +201,8 @@ model Is {
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
 
-  operasyonlar  IsOperasyon[]
-  workSchedule  WorkSchedule?
+  operasyonlar IsOperasyon[]
+  workSchedule WorkSchedule?
 }
 
 model IsOperasyon {
@@ -224,15 +229,15 @@ enum PhaseType {
 }
 
 model WorkSchedule {
-  id        String   @id @default(cuid())
-  isId      String   @unique
-  is        Is       @relation(fields: [isId], references: [id], onDelete: Cascade)
+  id   String @id @default(cuid())
+  isId String @unique
+  is   Is     @relation(fields: [isId], references: [id], onDelete: Cascade)
 
   startDate DateTime?
   endDate   DateTime?
   notes     String?
 
-  phases    SchedulePhase[]
+  phases SchedulePhase[]
 
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
@@ -249,28 +254,27 @@ model SchedulePhase {
   plannedStart DateTime?
   plannedEnd   DateTime?
 
-  isCompleted  Boolean   @default(false)
-  completedAt  DateTime?
-  completedBy  String?
+  isCompleted Boolean   @default(false)
+  completedAt DateTime?
+  completedBy String?
 
-  isOverridden Boolean   @default(false)
+  isOverridden Boolean @default(false)
   overrideNote String?
   notes        String?
 
-  createdAt    DateTime  @default(now())
-  updatedAt    DateTime  @updatedAt
-
-  @@unique([workScheduleId, phase])
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
 
   fazAtamalar FazAtama[]
 
+  @@unique([workScheduleId, phase])
   @@map("schedule_phases")
 }
 
 model Personel {
-  id         String   @id @default(cuid())
-  atolyeId   String
-  atolye     Atolye   @relation(fields: [atolyeId], references: [id])
+  id       String @id @default(cuid())
+  atolyeId String
+  atolye   Atolye @relation(fields: [atolyeId], references: [id])
 
   ad            String
   soyad         String
@@ -288,6 +292,8 @@ model Personel {
   updatedAt DateTime @updatedAt
 
   fazAtamalar FazAtama[]
+  user        User?
+  davetler    PersonelLoginInvite[]
 
   @@map("personel")
 }
@@ -305,3 +311,32 @@ model FazAtama {
   @@unique([schedulePhaseId, personelId])
   @@map("faz_atamalar")
 }
+
+model PersonelLoginInvite {
+  id           String    @id @default(cuid())
+  personelId   String
+  personel     Personel  @relation(fields: [personelId], references: [id], onDelete: Cascade)
+  token        String    @unique
+  allowedMenus String[]  @default([])
+  expiresAt    DateTime
+  usedAt       DateTime?
+  createdAt    DateTime  @default(now())
+
+  @@map("personel_login_invites")
+}
+
+model MenuPermission {
+  id        String   @id @default(cuid())
+  userId    String
+  menuKey   String
+  canView   Boolean  @default(false)
+  canCreate Boolean  @default(false)
+  canEdit   Boolean  @default(false)
+  canDelete Boolean  @default(false)
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+
+  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
+
+  @@unique([userId, menuKey])
+}
diff --git a/src/app/actions/schedule.ts b/src/app/actions/schedule.ts
index 0bf1857..3b079b1 100644
--- a/src/app/actions/schedule.ts
+++ b/src/app/actions/schedule.ts
@@ -14,10 +14,15 @@ async function authBilgisiAl(): Promise<{
   userId: string | null;
   atolyeId: string | null;
   email: string | null;
+  role: string | null;
+  personelId: string | null;
 }> {
   const cookieStore = await cookies();
   const token = cookieStore.get("metrix-token")?.value;
-  if (!token) return { userId: null, atolyeId: null, email: null };
+
+  if (!token) {
+    return { userId: null, atolyeId: null, email: null, role: null, personelId: null };
+  }
 
   try {
     const secret = new TextEncoder().encode(
@@ -27,17 +32,22 @@ async function authBilgisiAl(): Promise<{
     const { payload } = await jwtVerify(token, secret);
 
     const user = await prisma.user.findUnique({
-      where: { id: (payload as any).id },
-      include: { atolye: true },
+      where: { id: String((payload as any).id) },
+      include: {
+        atolye: true,
+        personel: true,
+      },
     });
 
     return {
       userId: user?.id || null,
-      atolyeId: user?.atolye?.id || null,
+      atolyeId: user?.atolye?.id || user?.personel?.atolyeId || null,
       email: user?.email || null,
+      role: user?.role || null,
+      personelId: user?.personelId || user?.personel?.id || null,
     };
   } catch {
-    return { userId: null, atolyeId: null, email: null };
+    return { userId: null, atolyeId: null, email: null, role: null, personelId: null };
   }
 }
 
@@ -46,16 +56,33 @@ async function atolyeIdAl(): Promise<string | null> {
   return auth.atolyeId;
 }
 
-export async function getSchedulesForMonth(year: number, month: number) {
-  const atolyeId = await atolyeIdAl();
-  if (!atolyeId) return [];
+export export async function getSchedulesForMonth(year: number, month: number) {
+  const auth = await authBilgisiAl();
+
+  if (!auth.atolyeId) return [];
 
   const startOfMonth = new Date(year, month - 1, 1);
   const endOfMonth = new Date(year, month, 0, 23, 59, 59);
 
+  const personelFiltresi =
+    auth.role !== "ADMIN" && auth.personelId
+      ? {
+          phases: {
+            some: {
+              fazAtamalar: {
+                some: {
+                  personelId: auth.personelId,
+                },
+              },
+            },
+          },
+        }
+      : {};
+
   return prisma.workSchedule.findMany({
     where: {
-      is: { atolyeId },
+      is: { atolyeId: auth.atolyeId },
+      ...personelFiltresi,
       OR: [
         { startDate: { gte: startOfMonth, lte: endOfMonth } },
         { endDate: { gte: startOfMonth, lte: endOfMonth } },
diff --git a/src/app/api/auth/login/route.ts b/src/app/api/auth/login/route.ts
index c4c0b4b..fdfd033 100644
--- a/src/app/api/auth/login/route.ts
+++ b/src/app/api/auth/login/route.ts
@@ -9,7 +9,10 @@ export async function POST(req: NextRequest) {
   try {
     const { email, password, beniHatirla } = await req.json()
 
-    const kullanici = await prisma.user.findUnique({ where: { email } })
+    const kullanici = await prisma.user.findUnique({
+      where: { email },
+      include: { personel: true },
+    })
     if (!kullanici) {
       return NextResponse.json({ hata: 'E-posta veya şifre hatalı.' }, { status: 401 })
     }
@@ -32,7 +35,13 @@ export async function POST(req: NextRequest) {
     const secret = new TextEncoder().encode(process.env.JWT_SECRET || 'metrix-gizli-anahtar-2024')
     const sureDakika = beniHatirla ? 60 * 24 * 30 : 60 * 24 // 30 gün veya 1 gün
 
-    const token = await new SignJWT({ id: kullanici.id, email: kullanici.email })
+    const token = await new SignJWT({
+      id: kullanici.id,
+      email: kullanici.email,
+      role: kullanici.role,
+      personelId: kullanici.personelId,
+      allowedMenus: kullanici.allowedMenus,
+    })
       .setProtectedHeader({ alg: 'HS256' })
       .setExpirationTime(`${sureDakika}m`)
       .sign(secret)
diff --git a/src/app/api/logo/route.ts b/src/app/api/logo/route.ts
index ce39c97..ca63700 100644
--- a/src/app/api/logo/route.ts
+++ b/src/app/api/logo/route.ts
@@ -1,63 +1,27 @@
-import { NextRequest, NextResponse } from 'next/server'
-import { PrismaClient } from '@prisma/client'
-import { cookies } from 'next/headers'
-import { jwtVerify } from 'jose'
-import { v2 as cloudinary } from 'cloudinary'
+import { NextRequest, NextResponse } from "next/server";
 
-const prisma = new PrismaClient()
-
-cloudinary.config({
-  cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
-  api_key: process.env.CLOUDINARY_API_KEY,
-  api_secret: process.env.CLOUDINARY_API_SECRET,
-})
-
-async function kullaniciAl() {
-  const cookieStore = await cookies()
-  const token = cookieStore.get('metrix-token')?.value
-  if (!token) return null
+export async function POST(req: NextRequest) {
   try {
-    const secret = new TextEncoder().encode(process.env.JWT_SECRET || 'metrix-gizli-anahtar-2024')
-    const { payload } = await jwtVerify(token, secret)
-    return payload as { id: string; email: string }
-  } catch {
-    return null
+    const formData = await req.formData();
+    const file = formData.get("file");
+
+    if (!file) {
+      return NextResponse.json({ error: "Dosya yok" }, { status: 400 });
+    }
+
+    // 🔥 Şimdilik mock (gerçek upload sonra)
+    const fakeUrl = "/uploads/logo.png";
+
+    return NextResponse.json({
+      success: true,
+      logoUrl: fakeUrl,
+    });
+  } catch (error) {
+    console.error("LOGO UPLOAD ERROR:", error);
+
+    return NextResponse.json(
+      { error: "Upload sırasında hata oluştu" },
+      { status: 500 }
+    );
   }
 }
-
-export async function POST(req: NextRequest) {
-  const kullanici = await kullaniciAl()
-  if (!kullanici) return NextResponse.json({ hata: 'Yetkisiz.' }, { status: 401 })
-
-  const formData = await req.formData()
-  const dosya = formData.get('logo') as File
-
-  if (!dosya) return NextResponse.json({ hata: 'Dosya bulunamadı.' }, { status: 400 })
-
-  const bytes = await dosya.arrayBuffer()
-  const buffer = Buffer.from(bytes)
-
-  const sonuc = await new Promise<{ secure_url: string }>((resolve, reject) => {
-    cloudinary.uploader.upload_stream(
-      {
-        folder: 'metrix-logolar',
-        public_id: `logo-${kullanici.id}`,
-        overwrite: true,
-        transformation: [{ width: 400, height: 200, crop: 'fit' }],
-      },
-      (hata, sonuc) => {
-        if (hata) reject(hata)
-        else resolve(sonuc as { secure_url: string })
-      }
-    ).end(buffer)
-  })
-
-  const logoUrl = sonuc.secure_url
-
-  await prisma.atolye.update({
-    where: { userId: kullanici.id },
-    data: { logoUrl },
-  })
-
-  return NextResponse.json({ logoUrl })
-}
\ No newline at end of file
diff --git a/src/app/api/personel/route.ts b/src/app/api/personel/route.ts
index 01ce315..e2ba7d0 100644
--- a/src/app/api/personel/route.ts
+++ b/src/app/api/personel/route.ts
@@ -2,6 +2,7 @@ import { NextRequest, NextResponse } from 'next/server'
 import { PrismaClient } from '@prisma/client'
 import { cookies } from 'next/headers'
 import { jwtVerify } from 'jose'
+import bcrypt from 'bcryptjs'
 
 const globalForPrisma = globalThis as unknown as {
   prisma?: PrismaClient
@@ -31,7 +32,7 @@ async function atolyeIdAl() {
     const { payload } = await jwtVerify(token, secret)
 
     const user = await prisma.user.findUnique({
-      where: { id: (payload as any).id },
+      where: { id: String((payload as any).id) },
       include: { atolye: true },
     })
 
@@ -42,6 +43,48 @@ async function atolyeIdAl() {
   }
 }
 
+function permissionPayload(veri: any) {
+  const y = veri?.yetkiler || {}
+
+  return [
+    {
+      menuKey: 'dashboard',
+      canView: false,
+      canCreate: false,
+      canEdit: false,
+      canDelete: false,
+    },
+    {
+      menuKey: 'is-programi',
+      canView: !!y.isProgramiGor,
+      canCreate: !!y.isOlustur,
+      canEdit: !!y.tamamlandiYap,
+      canDelete: false,
+    },
+    {
+      menuKey: 'teklifler',
+      canView: !!y.isOlustur,
+      canCreate: !!y.isOlustur,
+      canEdit: !!y.isOlustur,
+      canDelete: false,
+    },
+    {
+      menuKey: 'musteriler',
+      canView: !!y.musteriOlustur,
+      canCreate: !!y.musteriOlustur,
+      canEdit: !!y.musteriOlustur,
+      canDelete: false,
+    },
+    {
+      menuKey: 'tahsilat',
+      canView: !!y.tahsilatGir,
+      canCreate: !!y.tahsilatGir,
+      canEdit: !!y.tahsilatGir,
+      canDelete: false,
+    },
+  ]
+}
+
 export async function GET() {
   try {
     const atolyeId = await atolyeIdAl()
@@ -69,6 +112,15 @@ export async function GET() {
             },
           },
         },
+        user: {
+          select: {
+            id: true,
+            email: true,
+            aktif: true,
+            role: true,
+            menuPermissions: true,
+          },
+        },
       },
     })
 
@@ -139,21 +191,91 @@ export async function POST(req: NextRequest) {
 
     const veri = await req.json()
 
-    const personel = await prisma.personel.create({
-      data: {
-        atolyeId,
-        ad: veri.ad,
-        soyad: veri.soyad,
-        gorevi: veri.gorevi,
-        bagliOlduguId: veri.bagliOlduguId || null,
-        calismaYili: parseInt(veri.calismaYili) || 0,
-        telefon: veri.telefon || '',
-        email: veri.email || '',
-        aktif: true,
-      },
+    if (!veri.ad || !veri.soyad) {
+      return NextResponse.json(
+        { hata: 'Ad ve soyad zorunludur.' },
+        { status: 400 }
+      )
+    }
+
+    if (veri.loginAktif && (!veri.loginEmail || !veri.loginPassword)) {
+      return NextResponse.json(
+        { hata: 'Giriş yetkisi için e-posta ve şifre zorunludur.' },
+        { status: 400 }
+      )
+    }
+
+    const sonuc = await prisma.$transaction(async (tx) => {
+      const personel = await tx.personel.create({
+        data: {
+          atolyeId,
+          ad: veri.ad,
+          soyad: veri.soyad,
+          gorevi: veri.gorevi,
+          bagliOlduguId: veri.bagliOlduguId || null,
+          calismaYili: parseInt(veri.calismaYili) || 0,
+          telefon: veri.telefon || '',
+          email: veri.email || '',
+          aktif: true,
+        },
+      })
+
+      let user = null
+
+      if (veri.loginAktif) {
+        const mevcutUser = await tx.user.findUnique({
+          where: { email: veri.loginEmail },
+        })
+
+        if (mevcutUser) {
+          throw new Error('Bu giriş e-postası zaten kullanılıyor.')
+        }
+
+        const hash = await bcrypt.hash(veri.loginPassword, 10)
+
+        user = await tx.user.create({
+          data: {
+            email: veri.loginEmail,
+            password: hash,
+            ad: `${veri.ad} ${veri.soyad}`,
+            aktif: true,
+            role: 'USER',
+            personelId: personel.id,
+          },
+        })
+
+        const permissions = permissionPayload(veri)
+
+        for (const permission of permissions) {
+          await tx.menuPermission.upsert({
+            where: {
+              userId_menuKey: {
+                userId: user.id,
+                menuKey: permission.menuKey,
+              },
+            },
+            update: {
+              canView: permission.canView,
+              canCreate: permission.canCreate,
+              canEdit: permission.canEdit,
+              canDelete: permission.canDelete,
+            },
+            create: {
+              userId: user.id,
+              menuKey: permission.menuKey,
+              canView: permission.canView,
+              canCreate: permission.canCreate,
+              canEdit: permission.canEdit,
+              canDelete: permission.canDelete,
+            },
+          })
+        }
+      }
+
+      return { personel, user }
     })
 
-    return NextResponse.json({ personel })
+    return NextResponse.json(sonuc)
   } catch (error: any) {
     console.error('PERSONEL API POST HATASI:', error)
     return NextResponse.json(
@@ -178,6 +300,9 @@ export async function PUT(req: NextRequest) {
         id: veri.id,
         atolyeId,
       },
+      include: {
+        user: true,
+      },
     })
 
     if (!mevcut) {
@@ -187,21 +312,96 @@ export async function PUT(req: NextRequest) {
       )
     }
 
-    const personel = await prisma.personel.update({
-      where: { id: veri.id },
-      data: {
-        ad: veri.ad,
-        soyad: veri.soyad,
-        gorevi: veri.gorevi,
-        bagliOlduguId: veri.bagliOlduguId || null,
-        calismaYili: parseInt(veri.calismaYili) || 0,
-        telefon: veri.telefon || '',
-        email: veri.email || '',
-        aktif: veri.aktif ?? true,
-      },
+    const sonuc = await prisma.$transaction(async (tx) => {
+      const personel = await tx.personel.update({
+        where: { id: veri.id },
+        data: {
+          ad: veri.ad,
+          soyad: veri.soyad,
+          gorevi: veri.gorevi,
+          bagliOlduguId: veri.bagliOlduguId || null,
+          calismaYili: parseInt(veri.calismaYili) || 0,
+          telefon: veri.telefon || '',
+          email: veri.email || '',
+          aktif: veri.aktif ?? true,
+        },
+      })
+
+      if (veri.loginAktif) {
+        let user = mevcut.user
+
+        if (!user) {
+          if (!veri.loginEmail || !veri.loginPassword) {
+            throw new Error('Giriş yetkisi için e-posta ve şifre zorunludur.')
+          }
+
+          const hash = await bcrypt.hash(veri.loginPassword, 10)
+
+          user = await tx.user.create({
+            data: {
+              email: veri.loginEmail,
+              password: hash,
+              ad: `${veri.ad} ${veri.soyad}`,
+              aktif: true,
+              role: 'USER',
+              personelId: personel.id,
+            },
+          })
+        } else {
+          const updateData: any = {
+            email: veri.loginEmail || user.email,
+            ad: `${veri.ad} ${veri.soyad}`,
+            aktif: true,
+          }
+
+          if (veri.loginPassword) {
+            updateData.password = await bcrypt.hash(veri.loginPassword, 10)
+          }
+
+          await tx.user.update({
+            where: { id: user.id },
+            data: updateData,
+          })
+        }
+
+        const activeUser = await tx.user.findUnique({
+          where: { personelId: personel.id },
+        })
+
+        if (activeUser) {
+          const permissions = permissionPayload(veri)
+
+          for (const permission of permissions) {
+            await tx.menuPermission.upsert({
+              where: {
+                userId_menuKey: {
+                  userId: activeUser.id,
+                  menuKey: permission.menuKey,
+                },
+              },
+              update: {
+                canView: permission.canView,
+                canCreate: permission.canCreate,
+                canEdit: permission.canEdit,
+                canDelete: permission.canDelete,
+              },
+              create: {
+                userId: activeUser.id,
+                menuKey: permission.menuKey,
+                canView: permission.canView,
+                canCreate: permission.canCreate,
+                canEdit: permission.canEdit,
+                canDelete: permission.canDelete,
+              },
+            })
+          }
+        }
+      }
+
+      return { personel }
     })
 
-    return NextResponse.json({ personel })
+    return NextResponse.json(sonuc)
   } catch (error: any) {
     console.error('PERSONEL API PUT HATASI:', error)
     return NextResponse.json(
diff --git a/src/app/api/schedule/route.ts b/src/app/api/schedule/route.ts
index 2a3e9a5..ea70c8d 100644
--- a/src/app/api/schedule/route.ts
+++ b/src/app/api/schedule/route.ts
@@ -3,14 +3,20 @@ import { getSchedulesForMonth } from "@/app/actions/schedule";
 
 export async function GET(req: NextRequest) {
   const { searchParams } = req.nextUrl;
-  const year = parseInt(searchParams.get("year") ?? String(new Date().getFullYear()));
-  const month = parseInt(searchParams.get("month") ?? String(new Date().getMonth() + 1));
+
+  const year = parseInt(
+    searchParams.get("year") ?? String(new Date().getFullYear())
+  );
+
+  const month = parseInt(
+    searchParams.get("month") ?? String(new Date().getMonth() + 1)
+  );
 
   try {
     const schedules = await getSchedulesForMonth(year, month);
-    return NextResponse.json(schedules);
+    return NextResponse.json(Array.isArray(schedules) ? schedules : []);
   } catch (error) {
-    console.error(error);
-    return NextResponse.json({ error: "Veri alınamadı" }, { status: 500 });
+    console.error("SCHEDULE API HATASI:", error);
+    return NextResponse.json([]);
   }
 }
diff --git a/src/app/dashboard/personel/page.tsx b/src/app/dashboard/personel/page.tsx
index 8465d7e..ad2deaf 100644
--- a/src/app/dashboard/personel/page.tsx
+++ b/src/app/dashboard/personel/page.tsx
@@ -136,6 +136,28 @@ export default function PersonelSayfasi() {
     calismaYili: '',
     telefon: '',
     email: '',
+        loginAktif: false,
+        loginEmail: '',
+        loginPassword: '',
+        yetkiler: {
+          isProgramiGor: false,
+          sadeceKendiIsleri: false,
+          tamamlandiYap: false,
+          isOlustur: false,
+          musteriOlustur: false,
+          tahsilatGir: false,
+        },
+    loginAktif: false,
+    loginEmail: '',
+    loginPassword: '',
+    yetkiler: {
+      isProgramiGor: false,
+      sadeceKendiIsleri: false,
+      tamamlandiYap: false,
+      isOlustur: false,
+      musteriOlustur: false,
+      tahsilatGir: false,
+    },
   })
 
   useEffect(() => {
@@ -189,6 +211,17 @@ export default function PersonelSayfasi() {
         calismaYili: String(p.calismaYili ?? 0),
         telefon: p.telefon || '',
         email: p.email || '',
+        loginAktif: false,
+        loginEmail: p.email || '',
+        loginPassword: '',
+        yetkiler: {
+          isProgramiGor: false,
+          sadeceKendiIsleri: false,
+          tamamlandiYap: false,
+          isOlustur: false,
+          musteriOlustur: false,
+          tahsilatGir: false,
+        },
       })
     } else {
       setDuzenleId(null)
@@ -200,6 +233,28 @@ export default function PersonelSayfasi() {
         calismaYili: '',
         telefon: '',
         email: '',
+        loginAktif: false,
+        loginEmail: '',
+        loginPassword: '',
+        yetkiler: {
+          isProgramiGor: false,
+          sadeceKendiIsleri: false,
+          tamamlandiYap: false,
+          isOlustur: false,
+          musteriOlustur: false,
+          tahsilatGir: false,
+        },
+    loginAktif: false,
+    loginEmail: '',
+    loginPassword: '',
+    yetkiler: {
+      isProgramiGor: false,
+      sadeceKendiIsleri: false,
+      tamamlandiYap: false,
+      isOlustur: false,
+      musteriOlustur: false,
+      tahsilatGir: false,
+    },
       })
     }
     setFormAcik(true)
@@ -558,7 +613,7 @@ export default function PersonelSayfasi() {
 
       {formAcik && (
         <div
-          className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/55 p-4 backdrop-blur-sm"
+          className="fixed inset-0 z-[100] overflow-y-auto bg-slate-950/55 p-4 py-8 backdrop-blur-sm"
           onClick={() => setFormAcik(false)}
         >
           <div
@@ -657,6 +712,85 @@ export default function PersonelSayfasi() {
               </div>
             </div>
 
+            <div className="mt-8 rounded-3xl border border-slate-200 bg-slate-50 p-5 md:col-span-2">
+              <div className="flex items-start justify-between gap-4">
+                <div>
+                  <h3 className="text-base font-bold text-slate-900">Giriş ve Yetki Ayarları</h3>
+                  <p className="mt-1 text-sm text-slate-500">
+                    Bu personelin sisteme giriş yapıp yapamayacağını ve hangi işlemleri yapabileceğini belirle.
+                  </p>
+                </div>
+
+                <label className="flex items-center gap-2 rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700">
+                  <input
+                    type="checkbox"
+                    checked={form.loginAktif}
+                    onChange={(e) =>
+                      setForm((p) => ({ ...p, loginAktif: e.target.checked }))
+                    }
+                  />
+                  Giriş Yetkisi Ver
+                </label>
+              </div>
+
+              {form.loginAktif && (
+                <div className="mt-5 grid gap-4 md:grid-cols-2">
+                  <Field label="Giriş E-postası">
+                    <Input
+                      value={form.loginEmail}
+                      onChange={(e) =>
+                        setForm((p) => ({ ...p, loginEmail: e.target.value }))
+                      }
+                      placeholder="personel@firma.com"
+                    />
+                  </Field>
+
+                  <Field label="Geçici Şifre">
+                    <Input
+                      type="password"
+                      value={form.loginPassword}
+                      onChange={(e) =>
+                        setForm((p) => ({ ...p, loginPassword: e.target.value }))
+                      }
+                      placeholder="Personel şifresi"
+                    />
+                  </Field>
+                </div>
+              )}
+
+              <div className="mt-5 grid gap-3 md:grid-cols-2">
+                {[
+                  ["isProgramiGor", "İş programını görebilsin"],
+                  ["sadeceKendiIsleri", "Sadece kendisine atanmış işleri görsün"],
+                  ["tamamlandiYap", "Kendisine atanmış işi tamamlandı yapabilsin"],
+                  ["isOlustur", "Yeni iş oluşturabilsin"],
+                  ["musteriOlustur", "Müşteri açabilsin"],
+                  ["tahsilatGir", "Tahsilat girebilsin"],
+                ].map(([key, label]) => (
+                  <label
+                    key={key}
+                    className="flex items-center gap-3 rounded-2xl border border-slate-200 bg-white px-4 py-3 text-sm font-medium text-slate-700"
+                  >
+                    <input
+                      type="checkbox"
+                      checked={!!form.yetkiler[key as keyof typeof form.yetkiler]}
+                      onChange={(e) =>
+                        setForm((p) => ({
+                          ...p,
+                          yetkiler: {
+                            ...p.yetkiler,
+                            [key]: e.target.checked,
+                          },
+                        }))
+                      }
+                    />
+                    {label}
+                  </label>
+                ))}
+              </div>
+            </div>
+
+
             <div className="mt-8 flex flex-wrap justify-end gap-3">
               <button
                 type="button"
@@ -668,7 +802,7 @@ export default function PersonelSayfasi() {
 
               <button
                 type="button"
-                onClick={kaydet}
+                  onClick={kaydet}
                 disabled={kaydediliyor || !form.ad || !form.soyad}
                 className="rounded-2xl bg-gradient-to-r from-blue-600 to-violet-600 px-5 py-3 text-sm font-semibold text-white shadow-[0_12px_24px_rgba(59,130,246,0.22)] transition hover:scale-[1.01] disabled:opacity-60"
               >
diff --git a/src/components/dashboard/Sidebar.tsx b/src/components/dashboard/Sidebar.tsx
index 9430b24..c929589 100644
--- a/src/components/dashboard/Sidebar.tsx
+++ b/src/components/dashboard/Sidebar.tsx
@@ -9,20 +9,32 @@ type Atolye = {
   logoUrl?: string;
 };
 
+type CurrentUser = {
+  userId?: string | null;
+  email?: string;
+  role?: string;
+};
+
+type Permission = {
+  menuKey: string;
+  canView: boolean;
+};
+
 const menuItems = [
-  { href: "/dashboard", label: "Dashboard", badge: "Genel Bakış" },
-  { href: "/dashboard/isler", label: "İşler", badge: "Teklifler" },
-  { href: "/dashboard/musteriler", label: "Müşteriler", badge: "CRM" },
-  { href: "/dashboard/is-programi", label: "İş Programı", badge: "Planlama" },
-  { href: "/dashboard/atolye", label: "Atölye", badge: "Maliyet" },
-  { href: "/dashboard/personel", label: "Personel", badge: "Ekip" },
-  { href: "/dashboard/plaka-planlayici", label: "Plaka Planlayıcı", badge: "Optimizasyon" },
+  { href: "/dashboard", label: "Dashboard", badge: "Genel Bakış", key: "dashboard" },
+  { href: "/dashboard/isler", label: "İşler", badge: "Teklifler", key: "teklifler" },
+  { href: "/dashboard/musteriler", label: "Müşteriler", badge: "CRM", key: "musteriler" },
+  { href: "/dashboard/is-programi", label: "İş Programı", badge: "Planlama", key: "is-programi" },
+  { href: "/dashboard/atolye", label: "Atölye", badge: "Maliyet", key: "dashboard" },
+  { href: "/dashboard/personel", label: "Personel", badge: "Ekip", key: "personel" },
+  { href: "/dashboard/plaka-planlayici", label: "Plaka Planlayıcı", badge: "Optimizasyon", key: "dashboard" },
 ];
 
 export default function Sidebar() {
   const pathname = usePathname();
   const [atolye, setAtolye] = useState<Atolye | null>(null);
   const [mobileOpen, setMobileOpen] = useState(false);
+  const [visibleMenus, setVisibleMenus] = useState<typeof menuItems>([]);
 
   useEffect(() => {
     fetch("/api/atolye", { cache: "no-store" })
@@ -31,6 +43,50 @@ export default function Sidebar() {
       .catch(() => setAtolye(null));
   }, []);
 
+  useEffect(() => {
+    async function loadMenus() {
+      try {
+        const userRes = await fetch("/api/auth/current-user", {
+          cache: "no-store",
+          credentials: "include",
+        });
+
+        const user: CurrentUser = await userRes.json();
+
+        if (!user?.userId) {
+          setVisibleMenus([]);
+          return;
+        }
+
+        if (user.role === "ADMIN") {
+          setVisibleMenus(menuItems);
+          return;
+        }
+
+        const permRes = await fetch(
+          `/api/admin/menu-permissions?userId=${user.userId}`,
+          {
+            cache: "no-store",
+            credentials: "include",
+          }
+        );
+
+        const permissions: Permission[] = await permRes.json();
+
+        const allowedKeys = permissions
+          .filter((p) => p.canView)
+          .map((p) => p.menuKey);
+
+        setVisibleMenus(menuItems.filter((item) => allowedKeys.includes(item.key)));
+      } catch (e) {
+        console.error("SIDEBAR YETKİ HATASI:", e);
+        setVisibleMenus([]);
+      }
+    }
+
+    loadMenus();
+  }, []);
+
   async function logout() {
     try {
       await fetch("/api/auth/logout", {
@@ -81,36 +137,42 @@ export default function Sidebar() {
 
         <div className="mt-3 rounded-xl bg-gradient-to-br from-blue-500/20 via-violet-500/20 to-fuchsia-500/20 p-3">
           <p className="text-xs text-slate-300">Bugünün Özeti</p>
-          <p className="mt-1 text-xl font-bold leading-none">15 aktif teklif</p>
+          <p className="mt-1 text-xl font-bold leading-none">0 aktif teklif</p>
           <p className="mt-2 text-xs leading-snug text-slate-300">
-            4 onay, 11 takip bekliyor
+            Gerçek veriler girildikçe güncellenecek.
           </p>
         </div>
       </div>
 
       <nav className="mt-3 space-y-1">
-        {menuItems.map((item) => {
-          const active = pathname === item.href;
-
-          return (
-            <Link
-              key={item.href}
-              href={item.href}
-              onClick={() => mobile && setMobileOpen(false)}
-              className={[
-                "group flex items-center justify-between rounded-2xl border px-4 py-2.5 text-sm transition",
-                active
-                  ? "border-blue-400/50 bg-blue-500/15 text-white shadow-[0_0_0_1px_rgba(96,165,250,0.3)]"
-                  : "border-transparent text-slate-200 hover:border-white/10 hover:bg-white/5 hover:text-white",
-              ].join(" ")}
-            >
-              <span className="font-medium">{item.label}</span>
-              <span className="rounded-full bg-white/5 px-2.5 py-1 text-[11px] text-slate-300 transition group-hover:bg-white/10">
-                {item.badge}
-              </span>
-            </Link>
-          );
-        })}
+        {visibleMenus.length === 0 ? (
+          <div className="rounded-2xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-slate-300">
+            Yetkili menü bulunamadı.
+          </div>
+        ) : (
+          visibleMenus.map((item) => {
+            const active = pathname === item.href;
+
+            return (
+              <Link
+                key={item.href}
+                href={item.href}
+                onClick={() => mobile && setMobileOpen(false)}
+                className={[
+                  "group flex items-center justify-between rounded-2xl border px-4 py-2.5 text-sm transition",
+                  active
+                    ? "border-blue-400/50 bg-blue-500/15 text-white shadow-[0_0_0_1px_rgba(96,165,250,0.3)]"
+                    : "border-transparent text-slate-200 hover:border-white/10 hover:bg-white/5 hover:text-white",
+                ].join(" ")}
+              >
+                <span className="font-medium">{item.label}</span>
+                <span className="rounded-full bg-white/5 px-2.5 py-1 text-[11px] text-slate-300 transition group-hover:bg-white/10">
+                  {item.badge}
+                </span>
+              </Link>
+            );
+          })
+        )}
       </nav>
 
       <div className="mt-3 rounded-2xl border border-amber-400/20 bg-gradient-to-br from-amber-500/10 to-orange-500/10 p-3">
@@ -121,7 +183,7 @@ export default function Sidebar() {
           Takip Gerekiyor
         </h3>
         <p className="mt-2 text-xs leading-snug text-slate-300">
-          Bekleyen teklif sayısı yüksek. İlk 5 müşteriye bugün dönüş yap.
+          Bekleyen teklif ve işler gerçek veriye bağlanacak.
         </p>
       </div>
 
@@ -137,7 +199,6 @@ export default function Sidebar() {
 
   return (
     <>
-      {/* MOBILE MENU BUTTON */}
       <button
         type="button"
         onClick={() => setMobileOpen(true)}
@@ -147,7 +208,6 @@ export default function Sidebar() {
         ☰
       </button>
 
-      {/* MOBILE DRAWER */}
       {mobileOpen && (
         <div className="fixed inset-0 z-[100] md:hidden">
           <div
@@ -177,7 +237,6 @@ export default function Sidebar() {
         </div>
       )}
 
-      {/* DESKTOP SIDEBAR */}
       <aside className="hidden md:fixed md:inset-y-0 md:left-0 md:flex md:w-72 bg-[#0B1020] text-white border-r border-white/10">
         <div className="flex h-full w-full min-h-0 flex-col p-4">
           <SidebarContent />
